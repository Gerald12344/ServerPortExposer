{
  "name": "tunnelmanager",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildMain": "npx tsc",
    "start": "npm run buildMain && node dist/index.js --trace-deprecation",
    "dev": "npx tsc && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^18.11.15",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "mongodb": "^4.12.1"
  }
}
